The scripts in this folder are used by server and client, they are in the "sh" namespace.

IMPORTANT

1) The client should reference the scripts in alphabetical order (index.js last).
2) Each file except 00_init.js must start with the following code,
    were previous-file-name is the alphabetically previous file name:

/*global require, exports, module*/
var sh = require('./previous-file-name'), _ = sh._;
if (exports !== undefined) {
    /**
     * exports from NodeJS
     * @type {*}
     */
   sh = module.exports = sh;
}

